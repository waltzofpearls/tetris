[program:tetris]
autorestart=true
autostart=true
command=docker run
    --restart no
    --name tetris
    -a stdout
    -a stderr
    --rm true
    -p 49001:3000
    -e TETRIS_GITHUB_TOKEN=[Change this to the real github token]
    waltzofpearls/tetris